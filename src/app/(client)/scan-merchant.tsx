import { Screen, Text } from '@/components/ui';

export default function ClientScreen() {
  return (
    <Screen>
      <Text variant="title">scan-merchant</Text>
      <Text tone="secondary">Écran client — implémenté à l’étape 5.</Text>
    </Screen>
  );
}
